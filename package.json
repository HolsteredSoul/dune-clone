{
  "name": "dune-clone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "sim": "esbuild scripts/sim.ts --bundle --platform=node --format=esm --outfile=scripts/sim.mjs && node scripts/sim.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
